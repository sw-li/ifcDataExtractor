"""
extractor package — IFC data extraction modules.

Available modules:
  metadata   — IFC header & IfcProject info
  hierarchy  — spatial decomposition tree (Site > Building > Storey > Space > Element)
  psets      — Pset_* property sets (IfcPropertySingleValue)
  quantities — Qto_* quantity sets (IfcElementQuantity)
"""
